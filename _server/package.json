{
	"name": "server",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node serverListen.js",
		"devServer": "nodemon serverListen.js"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"config": "^3.2.4",
		"express": "^4.17.1",
		"mongoose": "^5.8.1"
	},
	"devDependencies": {
		"nodemon": "^2.0.2",
		"supertest": "^4.0.2"
	}
}
